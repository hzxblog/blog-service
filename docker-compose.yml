version: "3"
services:
  apiapp:
    build: ./
    ports:
      - "7000:7000"
    links: 
      - mongo
  mongo:
    image: mongo
    volumes:
      - /data/mongodb/db:/data/db
    ports:
      - "27017:27017"
